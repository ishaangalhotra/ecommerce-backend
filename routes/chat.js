const express = require('express');
const router = express.Router();

// Placeholder chat route - to be implemented
router.get('/', (req, res) => {
  res.json({ message: 'Chat feature coming soon' });
});

module.exports = router;
