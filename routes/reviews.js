const express = require('express');
const router = express.Router();

// Placeholder reviews route - to be implemented
router.get('/', (req, res) => {
  res.json({ message: 'Reviews feature coming soon' });
});

module.exports = router;
